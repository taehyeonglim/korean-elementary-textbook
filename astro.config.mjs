import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://taehyeonglim.github.io",
  base: "/korean-elementary-textbook",
  output: "static",
  trailingSlash: "always",
});
